import { Paper, Typography } from '@mui/material'

export default function PoliciesPage() {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h6">Policies</Typography>
      <Typography sx={{ mt: 1 }}>Reglas del Site (morosos, horarios, delivery) (stub)</Typography>
    </Paper>
  )
}
