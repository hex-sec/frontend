import { Paper, Typography } from '@mui/material'

export default function VehiclesPage() {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h6">Vehicles</Typography>
      <Typography sx={{ mt: 1 }}>Búsqueda por placa y CRUD (stub)</Typography>
    </Paper>
  )
}
