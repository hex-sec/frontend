import { Paper, Typography } from '@mui/material'

export default function ReportsPage() {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h6">Reports</Typography>
      <Typography sx={{ mt: 1 }}>Filtros, métricas, export a CSV/PDF (stub)</Typography>
    </Paper>
  )
}
