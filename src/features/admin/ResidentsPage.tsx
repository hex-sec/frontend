import { Paper, Typography } from '@mui/material'

export default function ResidentsPage() {
  return (
    <Paper sx={{ p: 3 }}>
      <Typography variant="h6">Residents</Typography>
      <Typography sx={{ mt: 1 }}>Listado y CRUD de residentes (stub)</Typography>
    </Paper>
  )
}
